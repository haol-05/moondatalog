# MoonDatalog 项目申报书

## 1. 项目名称和 GitHub 仓库地址

- 项目名称：MoonDatalog —— 带形式化验证的 Datalog 查询引擎
- GitHub 仓库：https://github.com/haol-05/moondatalog

## 2. 项目简介

MoonDatalog 是一个用纯 MoonBit 实现的 Datalog 逻辑查询引擎，定位为**可嵌入、可复用、经过机器校验的查询内核**，面向图查询、静态分析、数据血缘追踪与规则引擎等场景。除基础的事实 / 规则 / 查询与递归（半朴素求值）外，本项目重点补齐了**分层否定**、**聚合**、**算术比较**与**求值前静态检查**等工程实际必需的能力，并借助 MoonBit 的 **`moon prove` 形式化验证**对核心纯函数附加逻辑契约、由 SMT 求解器机器校验正确性。项目已发布到 mooncakes.io，附带完整测试（50 个用例）、CI、命令行工具与示例。

## 3. 项目方向与适用场景

- 项目方向：数据处理与数据库（Datalog 逻辑查询引擎）、形式化验证（对应大赛推荐方向「moon prove 验证的通用算法库」）
- 适用场景：图查询（传递闭包、可达性）、静态程序分析、配置校验、业务规则引擎、数据血缘追踪

## 4. 拟实现的核心功能

1. 事实 / 规则 / 查询语法与递归（传递闭包），bottom-up 半朴素求值；
2. 分层否定 `not p(...)`：基于 Tarjan 强连通分量自动分层，保证语义良定义；
3. Soufflé 风格聚合 `count / sum / min / max / avg`；
4. 算术与比较（`+ - * / %`、`= != < <= > >=`，整数与浮点）；
5. 求值前静态检查：未定义谓词、元数不一致、不安全规则、不可分层否定、除零等；
6. 形式化验证：`moon prove` 机器校验 `clamp`、`bounded_sum`、`index_of_max` 等核心纯函数（前置 / 后置条件、循环不变量）；
7. 工程化交付：mooncakes.io 发布、50 个测试用例、GitHub Actions CI、命令行工具与示例程序。

## 5. 是否为原创项目、移植项目或参考已有开源项目

本项目为**独立原创实现**：参考 Datalog 语言的公开规范与学术文献，自研词法分析、语法分析、分层与求值引擎，未复制或移植任何现有代码，不涉及第三方许可证问题，采用 Apache-2.0 开源许可证（OSI 认可）。

**说明**：MoonBit 生态中另有若干逻辑编程方向的早期探索（如 Prolog / miniKanren 等逻辑语言，以及未发布到 mooncakes 的早期 Datalog 原型）。本项目与之不同点在于：定位为**完整可用的 Datalog 查询内核**，补齐了分层否定、聚合、算术与静态检查等能力，并新增**形式化验证**与完整工程化交付（发布、测试、CI、文档），属于在生态基础上的独立贡献与能力补全。
