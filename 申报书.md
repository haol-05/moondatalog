# MoonDatalog 项目申报书

## 1. 项目名称和 GitHub 仓库地址

- **项目名称**：MoonDatalog —— 纯 MoonBit 实现的 Datalog 查询引擎
- **GitHub 仓库地址**：https://github.com/haol-05/moondatalog

## 2. 项目简介

MoonDatalog 是一个用纯 MoonBit 实现的 **Datalog 声明式逻辑查询引擎**。Datalog 是一种逻辑查询语言，广泛用于图分析、静态分析、数据血缘追踪与规则引擎等场景。目前 MoonBit 生态在「逻辑查询引擎」方向尚属空白，本项目旨在补上这一能力，为 MoonBit 开发者提供一个可嵌入、可复用的查询内核。

## 3. 项目方向与适用场景

- **项目方向**：数据处理与数据库（逻辑查询引擎）
- **适用场景**：图查询（传递闭包、可达性、最短路径）、静态程序分析、配置校验、业务规则引擎、数据血缘追踪

## 4. 拟实现的核心功能

1. 事实 / 规则 / 查询语法（`p(...).`、`h(...) :- b.`、`?- b.`）与递归（传递闭包）；
2. 算术与比较（`+ - * / %`、`= != < <= > >=`，整数与浮点）；
3. 分层否定（`not p(...)`），基于 Tarjan 强连通分量自动分层；
4. Soufflé 风格聚合（`count / sum / min / max / avg`）；
5. bottom-up 半朴素（semi-naive）求值引擎；
6. 求值前静态检查：未定义谓词、元数不一致、不安全规则、不可分层否定、除零等；
7. 命令行工具与示例程序，47 个测试用例，CI 集成，发布到 mooncakes.io。

## 5. 是否为原创项目、移植项目或参考已有开源项目

本项目为 **原创项目**，非移植、非参考其他开源项目。采用 **Apache-2.0** 开源许可证（OSI 认可）。
