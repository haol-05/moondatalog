# MoonDatalog 项目申报书

## 1. 项目名称和 GitHub 仓库地址

- 项目名称：MoonDatalog —— 带形式化验证的 Datalog 查询引擎
- GitHub 仓库：https://github.com/haol-05/moondatalog

## 2. 项目简介

纯 MoonBit 实现的 Datalog 逻辑查询引擎，定位为可嵌入、可复用的查询内核，面向图查询、静态分析、规则引擎等场景。具备递归（半朴素求值）、分层否定、聚合、算术比较与求值前静态检查；核心函数经 `moon prove` 形式化验证；已发布 mooncakes.io，含 50 个测试与 CI。

## 3. 项目方向与适用场景

- 项目方向：数据处理与数据库、形式化验证
- 适用场景：图查询（传递闭包、可达性）、静态分析、配置校验、规则引擎、数据血缘追踪

## 4. 拟实现的核心功能

1. 事实/规则/查询与递归，bottom-up 半朴素求值；
2. 分层否定 `not`：Tarjan SCC 自动分层；
3. 聚合 `count/sum/min/max/avg`；
4. 算术与比较（`+ - * / %`、`= != < <= > >=`，整数与浮点）；
5. 求值前静态检查（未定义谓词、元数、安全性、除零等）；
6. 形式化验证：`moon prove` 机器校验核心纯函数；
7. 工程化交付：mooncakes.io 发布、50 测试、GitHub Actions CI、CLI 与示例。

## 5. 是否为原创项目、移植项目或参考已有开源项目

独立原创实现（Apache-2.0），参考 Datalog 公开规范自研，未移植或复制任何现有代码。生态中另有逻辑编程方向的早期探索（Prolog/miniKanren、未发布的早期 Datalog 原型），本项目定位为完整可用的 Datalog 查询内核，补齐分层否定、聚合、算术与静态检查，并新增形式化验证，属独立贡献。

**备注**：本项目为重新提交版本，已按初审意见改为规范 Markdown 并补充生态重合说明，新增 `moon prove` 形式化验证能力。
